RSpec.describe Mongoid::Bulk::Import do
  it "has a version number" do
    expect(Mongoid::Bulk::Import::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
